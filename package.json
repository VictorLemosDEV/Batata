{
  "name": "teste",
  "version": "1.0.1",
  "description": "a",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build  --win --publish always"
  },
  "author": "Victor",
  "license": "MIT",
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-builder": "^24.12.0"
  },
  "dependencies": {
    "electron-updater": "^6.1.8"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/VictorLemosDEV/Batata.git"
  }
}
